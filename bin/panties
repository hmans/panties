#!/usr/bin/env ruby
require 'panties'

client = Panties::Client.new('http://andy.pants.dev')
posts = client.posts

posts.first(5).each do |post|
  puts "POST: #{post['url']}\n\n"
  puts post['body']
  puts "\n\n\n"
end
